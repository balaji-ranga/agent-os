{
  "template_key": "video-media",
  "name": "Video: W-Media (Veo clips)",
  "description": "Phase 2 stub — plan id → generate_video (Replicate google/veo-*) per scene → asset manifest. Expand graph when S4 ships. Chat: run video media",
  "chat_trigger_phrase": "run video media",
  "trigger_modes": [
    "manual",
    "chat"
  ],
  "workflow_id_pattern": "video-media-{ownerSlug}",
  "kind": "video_content",
  "phase": 2,
  "status": "stub",
  "maintained_in": "company-blueprints/standard/video-content/workflow-media.json",
  "notes": [
    "Wire tool nodes to generate_video with Tools→Model override for google/veo-*.",
    "Persist paths only under media/generated/{ceo}/; write video_jobs rows.",
    "Orchestrator triggers after storyboard approval — do not invent success."
  ],
  "graph": {
    "nodes": [
      {
        "id": "trigger-1",
        "type": "trigger",
        "position": { "x": 40, "y": 200 },
        "data": {
          "label": "Start",
          "triggerModes": ["manual", "chat"],
          "chatPhrase": "run video media"
        }
      },
      {
        "id": "brain-stub",
        "type": "brain",
        "position": { "x": 280, "y": 200 },
        "data": {
          "label": "Stub — implement S4",
          "prompt": "W-Media is not fully implemented yet. Reply with: status=stub, next=implement generate_video scene loop from storyboard_id in input. Do not claim videos were generated."
        }
      }
    ],
    "edges": [
      { "id": "e1", "source": "trigger-1", "target": "brain-stub" }
    ]
  }
}
