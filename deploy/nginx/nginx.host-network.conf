upstream agent_os_backend {
    server 127.0.0.1:3001;
    keepalive 8;
}

upstream agent_os_frontend {
    server 127.0.0.1:8080;
    keepalive 4;
}

upstream agent_os_openclaw {
    server 127.0.0.1:18789;
    keepalive 4;
}

# Access logs omit query strings on /api so internal_token / webhook secrets never hit disk.
log_format agent_os_api '$remote_addr - $remote_user [$time_local] '
                        '"$request_method $uri HTTP/$server_protocol" $status '
                        '$body_bytes_sent rt=$request_time '
                        '"$http_user_agent"';

log_format agent_os_default '$remote_addr - $remote_user [$time_local] '
                            '"$request_method $uri HTTP/$server_protocol" $status '
                            '$body_bytes_sent rt=$request_time '
                            '"$http_referer" "$http_user_agent"';

# ---------------------------------------------------------------------------
# Apex marketing site — flolah.cloud + www.flolah.cloud (static homepage)
# CRM must NOT share these names. Twenty lives on crm.flolah.cloud only.
# ---------------------------------------------------------------------------
server {
    listen 80;
    listen [::]:80;
    server_name flolah.cloud www.flolah.cloud;

    location /health {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name flolah.cloud www.flolah.cloud;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    client_max_body_size 32m;
    access_log /var/log/nginx/access.log agent_os_default;

    root /usr/share/nginx/flolah-home;
    index index.html;

    location = / {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files /index.html =404;
    }

    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files /index.html =404;
    }

    # Marketing subpages (must not fall through to the SPA proxy)
    location = /vision {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files /vision.html =404;
    }
    location = /vision.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files /vision.html =404;
    }

    # Legal pack (Terms, Privacy, Cookies, Open source) — not SPA
    location ^~ /legal/ {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files $uri $uri/ =404;
    }

    # Marketing assets first; unknown /assets/* fall through to the SPA build
    location /assets/ {
        add_header Cache-Control "public, max-age=604800";
        try_files $uri @apex_frontend;
    }

    location /api/ {
        access_log /var/log/nginx/api.access.log agent_os_api;
        proxy_pass http://agent_os_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 900s;
        proxy_send_timeout 900s;
    }

    location /openconnector/ {
        access_log /var/log/nginx/api.access.log agent_os_api;
        proxy_pass http://agent_os_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Cookie $http_cookie;
        proxy_read_timeout 120s;
        proxy_buffering off;
    }

    location /opensearch/ {
        access_log /var/log/nginx/api.access.log agent_os_api;
        proxy_pass http://agent_os_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Cookie $http_cookie;
        proxy_read_timeout 120s;
        proxy_buffering off;
    }

    location ~ ^/(overview|providers|actions|runs|access|resources)(/|$) {
        return 302 /openconnector$request_uri;
    }

    location /health {
        proxy_pass http://agent_os_backend/health;
        access_log off;
    }

    # OpenClaw Chrome extension relay (not the main gateway control WS).
    # The loopback relay rejects non-loopback Host headers (DNS-rebinding guard),
    # so we rewrite Host and path: /browser/extension → /extension on :18792.
    location /browser/extension {
        access_log /var/log/nginx/api.access.log agent_os_api;
        proxy_pass http://127.0.0.1:18792/extension;
        proxy_http_version 1.1;
        proxy_set_header Host 127.0.0.1:18792;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Sec-WebSocket-Protocol $http_sec_websocket_protocol;
        proxy_set_header Origin $http_origin;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_buffering off;
        proxy_request_buffering off;
    }

    # Prefer login.flolah.cloud for app deep links; keep SPA reachable on apex
    # until DNS + cert cover the login subdomain (see vps-expand-login-cert.sh).
    location / {
        proxy_pass http://agent_os_frontend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location @apex_frontend {
        proxy_pass http://agent_os_frontend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ---------------------------------------------------------------------------
# App + login — login.flolah.cloud
# ---------------------------------------------------------------------------
server {
    listen 80;
    listen [::]:80;
    server_name login.flolah.cloud;

    location /health {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name login.flolah.cloud;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    client_max_body_size 32m;
    access_log /var/log/nginx/access.log agent_os_default;

    location /api/ {
        access_log /var/log/nginx/api.access.log agent_os_api;
        proxy_pass http://agent_os_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 900s;
        proxy_send_timeout 900s;
    }

    # OpenConnector console (admin cookie) + OAuth callbacks — proxied via backend BFF
    location /openconnector/ {
        access_log /var/log/nginx/api.access.log agent_os_api;
        proxy_pass http://agent_os_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Cookie $http_cookie;
        proxy_read_timeout 120s;
        proxy_buffering off;
    }

    # OpenSearch Dashboards (admin cookie) — proxied via backend BFF; never expose :5601/:9200
    location /opensearch/ {
        access_log /var/log/nginx/api.access.log agent_os_api;
        proxy_pass http://agent_os_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Cookie $http_cookie;
        proxy_read_timeout 120s;
        proxy_buffering off;
    }

    # If the OC SPA escapes to site-root routes, bounce back under /openconnector
    location ~ ^/(overview|providers|actions|runs|access|resources)(/|$) {
        return 302 /openconnector$request_uri;
    }

    location /health {
        proxy_pass http://agent_os_backend/health;
        access_log off;
    }

    # Legal pack (static) — same documents as apex marketing /legal/
    location ^~ /legal/ {
        root /usr/share/nginx/flolah-home;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files $uri =404;
    }

    # OpenClaw Chrome extension relay (not the main gateway control WS).
    # The loopback relay rejects non-loopback Host headers (DNS-rebinding guard),
    # so we rewrite Host and path: /browser/extension → /extension on :18792.
    location /browser/extension {
        access_log /var/log/nginx/api.access.log agent_os_api;
        proxy_pass http://127.0.0.1:18792/extension;
        proxy_http_version 1.1;
        proxy_set_header Host 127.0.0.1:18792;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Sec-WebSocket-Protocol $http_sec_websocket_protocol;
        proxy_set_header Origin $http_origin;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_buffering off;
        proxy_request_buffering off;
    }

    # -------------------------------------------------------------------------
    # Legacy path embed — CRM host root only (subpath SPA does not work)
    location = /crm-app {
        return 301 https://crm.flolah.cloud/;
    }
    location ^~ /crm-app/ {
        return 301 https://crm.flolah.cloud/;
    }

    location / {
        proxy_pass http://agent_os_frontend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ---------------------------------------------------------------------------
# Business Core — Twenty CRM TLS proxy (iframe-safe HTTPS, same cert as login)
# Frontend embed: https://login.flolah.cloud:8443  (TWENTY_EMBED_URL)
# Upstream is host-bound compose port 127.0.0.1:3100 → twenty-server:3000
# ---------------------------------------------------------------------------
server {
    listen 8443 ssl http2;
    listen [::]:8443 ssl http2;
    server_name login.flolah.cloud;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    client_max_body_size 32m;
    access_log /var/log/nginx/access.log agent_os_default;

    # Allow Flolah SPA (login.flolah.cloud :443) to iframe Twenty
    proxy_hide_header Content-Security-Policy;
    proxy_hide_header X-Frame-Options;
    add_header Content-Security-Policy "frame-ancestors https://login.flolah.cloud https://flolah.cloud https://www.flolah.cloud 'self'" always;
    add_header X-Frame-Options "" always;

    location / {
        proxy_pass http://127.0.0.1:3100;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
        proxy_buffering off;
    }
}

# ---------------------------------------------------------------------------
# Business Core — ERPNext TLS proxy (iframe-safe HTTPS)
#
# Preferred public origin (Hostinger/cloud often allow only :443, not :8444):
#   ERPNEXT_EMBED_URL=https://erp.crm.flolah.cloud
# (uses existing DNS wildcard *.crm.flolah.cloud — exact server_name wins over CRM *.crm)
# Also: https://erp.flolah.cloud once apex ERP A-record + cert SAN exist.
# Legacy :8444 kept for local/lab when the edge permits alternate ports.
# Upstream 127.0.0.1:8085 when optional-erpnext is up
# ---------------------------------------------------------------------------

# Exact host before CRM *.crm.flolah.cloud catch-all (nginx prefers exact match).
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name erp.crm.flolah.cloud erp.flolah.cloud;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    client_max_body_size 32m;
    access_log /var/log/nginx/access.log agent_os_default;

    proxy_hide_header Content-Security-Policy;
    proxy_hide_header X-Frame-Options;
    add_header Content-Security-Policy "frame-ancestors https://login.flolah.cloud https://flolah.cloud https://www.flolah.cloud 'self'" always;
    add_header X-Frame-Options "" always;

    location = /flolah-erp-handoff {
        return 301 /flolah-erp-handoff/$is_args$args;
    }
    location ^~ /flolah-erp-handoff/ {
        alias /usr/share/nginx/erp-handoff/;
        index index.html;
        default_type text/html;
        add_header Cache-Control "no-store" always;
    }

    # Same-origin SSO: Set-Cookie (Secure; SameSite=None) + 302 Desk — works in Flolah iframe
    location = /flolah-erp-sso {
        proxy_pass http://127.0.0.1:3001/api/business-core/erp-sso-apply;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_hide_header X-Frame-Options;
        proxy_hide_header Content-Security-Policy;
        add_header Content-Security-Policy "frame-ancestors https://login.flolah.cloud https://flolah.cloud https://www.flolah.cloud 'self'" always;
        add_header X-Frame-Options "" always;
    }

    location / {
        proxy_pass http://127.0.0.1:8085;
        proxy_http_version 1.1;
        # Frappe site name from create-site (ERPNEXT_SITE_NAME), not request Host
        proxy_set_header Host frontend;
        proxy_set_header X-Frappe-Site-Name frontend;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
        proxy_buffering off;
    }
}

server {
    listen 8444 ssl http2;
    listen [::]:8444 ssl http2;
    server_name login.flolah.cloud erp.crm.flolah.cloud erp.flolah.cloud;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    client_max_body_size 32m;
    access_log /var/log/nginx/access.log agent_os_default;

    proxy_hide_header Content-Security-Policy;
    proxy_hide_header X-Frame-Options;
    add_header Content-Security-Policy "frame-ancestors https://login.flolah.cloud https://flolah.cloud https://www.flolah.cloud 'self'" always;

    location = /flolah-erp-handoff {
        return 301 /flolah-erp-handoff/$is_args$args;
    }
    location ^~ /flolah-erp-handoff/ {
        alias /usr/share/nginx/erp-handoff/;
        index index.html;
        default_type text/html;
        add_header Cache-Control "no-store" always;
    }

    location = /flolah-erp-sso {
        proxy_pass http://127.0.0.1:3001/api/business-core/erp-sso-apply;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_hide_header X-Frame-Options;
        proxy_hide_header Content-Security-Policy;
        add_header Content-Security-Policy "frame-ancestors https://login.flolah.cloud https://flolah.cloud https://www.flolah.cloud 'self'" always;
    }

    location / {
        proxy_pass http://127.0.0.1:8085;
        proxy_http_version 1.1;
        proxy_set_header Host frontend;
        proxy_set_header X-Frappe-Site-Name frontend;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
        proxy_buffering off;
    }
}

# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# Twenty CRM — dedicated subdomain crm.flolah.cloud (host root SPA).
# Never bind CRM to flolah.cloud / www.flolah.cloud (marketing static).
# TWENTY_SERVER_URL / TWENTY_EMBED_URL = https://crm.flolah.cloud
# Needs DNS A/CNAME + cert SAN (bash deploy/scripts/vps-expand-crm-cert.sh).
# ---------------------------------------------------------------------------
server {
    listen 80;
    listen [::]:80;
    server_name crm.flolah.cloud *.crm.flolah.cloud;

    location /health {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# Platform CRM apex (bootstrap / legacy). Multi-workspace UIs use {sub}.crm.flolah.cloud.
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name crm.flolah.cloud *.crm.flolah.cloud;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    client_max_body_size 32m;
    access_log /var/log/nginx/access.log agent_os_default;

    proxy_hide_header Content-Security-Policy;
    proxy_hide_header X-Frame-Options;
    add_header Content-Security-Policy "frame-ancestors https://login.flolah.cloud https://flolah.cloud https://www.flolah.cloud 'self'" always;
    add_header X-Frame-Options "" always;

    location = /flolah-handoff {
        return 301 /flolah-handoff/$is_args$args;
    }
    location ^~ /flolah-handoff/ {
        alias /usr/share/nginx/crm-handoff/;
        index index.html;
        default_type text/html;
        add_header Cache-Control "no-store" always;
        # Best-effort clear of common non-partitioned cookies (HttpOnly still
        # needs Twenty sign-out; localStorage wipe runs in the handoff page).
        add_header Set-Cookie "tokenPair=; Max-Age=0; Path=/; Secure; SameSite=Lax" always;
        add_header Set-Cookie "accessToken=; Max-Age=0; Path=/; Secure; SameSite=Lax" always;
        add_header Set-Cookie "refreshToken=; Max-Age=0; Path=/; Secure; SameSite=Lax" always;
    }

    location / {
        proxy_pass http://127.0.0.1:3100;
        proxy_http_version 1.1;
        # Pass workspace subdomain host so Twenty multi-workspace can resolve origin
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
        proxy_buffering off;
    }
}

# erp.flolah.cloud / erp.crm.flolah.cloud are defined in the ERPNext block above
# (exact names + Host: frontend). Do not reintroduce a host-as-site-name proxy.
